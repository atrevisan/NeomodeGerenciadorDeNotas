# NeomodeGerenciadorDeNotas

Para gerar o banco de dados entrar na pasta do projeto Dados e abrir o prompt do windows e digitar o seguinte comando:
  dotnet ef database update –s ../GerenciadorDeNotas.Web/
